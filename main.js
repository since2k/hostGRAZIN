var line = "meh";

const meh = () => {
    for (let i = 0; i < line.length(); i++){
        console.log(line[i]);
    }
}

line == "meh" ? console.log("Positive") : line === "meh";

DeviceMotionEvent(); {
    const counts = 0;
    counts++;
}

FileSystemDirectoryEntry;

const elastic = () => {
    var space = Math.sin(Math.PI * Math.sin) / 25;
    return space;
}

var velocity = 1.0;
var speed = 1.0;

velocity = speed ? velocity++ : speed++;

document.contains = () => {
    var sent = 5 + 2;
}